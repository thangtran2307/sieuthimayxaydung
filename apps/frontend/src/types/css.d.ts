// Ambient declarations for CSS side-effect imports (e.g. `import './globals.css'`).
// Next.js handles the actual CSS at bundle time; this only satisfies TypeScript's module resolution.
declare module '*.css';
